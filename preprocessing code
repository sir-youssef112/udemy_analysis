import pandas as pd
import sklearn as sn

df=pd.read_csv("udemy_courses.csv")

num_of_nulls=df.isna().sum()
shape=df.shape
columns_name=df.columns
num_of_duplicates=df.duplicated().sum()


print(df.head())
print('*'*50)
print(f"columns name:\n{columns_name}")
print('*'*50)
print(f"number of nulls:\n{num_of_nulls}")
print('*'*50)
print(f"number of duplicates:\n{num_of_duplicates}")
print('*'*50)
print(f"number of rows and columns: {shape}")
print('*'*50)
print(df.dtypes)
print('*'*50)
def analyze_outliers(df):
    numeric_cols = df.select_dtypes(include=['int64', 'float64']).columns

    for col in numeric_cols:
        Q1 = df[col].quantile(0.25)
        Q3 = df[col].quantile(0.75)
        IQR = Q3 - Q1

        lower = Q1 - 1.5 * IQR
        upper = Q3 + 1.5 * IQR

        outliers = df[(df[col] < lower) | (df[col] > upper)]

        print(f"\n===== {col} =====")
        print(f"Lower: {lower}, Upper: {upper}")
        print(f"outliers: {outliers.shape[0]}")

        print("Values:")
        print(outliers[col].sort_values())


print(f"check on duplicates rows:\n{df[df.duplicated()]}")


cols = df.select_dtypes(include='object').columns
for col in cols:
    if df[col].dtype == 'object':
        df[col] = df[col].str.strip()

print(df.shape)

df.to_csv("udemy_courses_cleand.csv", index=False)
